interface:
  display_name: "Start.spring.io"
  short_description: "Repo-specific patterns and workflows for start.spring.io"
  default_prompt: "Use the start.spring.io repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true